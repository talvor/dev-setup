# Omarchy (Arch Linux). Sourced by ~/.zshrc.
#
# UNTESTED: this OS had no zsh config before. Nothing here yet; put anything
# that only applies to Omarchy in this file. Set DEV_SETUP_TMUX_SESSION to
# start a tmux session in every terminal (see ~/.zshrc.d/95_tmux.zshrc).
